{
  "servers": {
    "agent-memory": {
      "type": "stdio",
      "command": "python",
      "args": ["${workspaceFolder}/core/tools/memory_mcp.py"],
      "env": {
        "AGENT_MEMORY_ROOT": "${workspaceFolder}"
      }
    }
  }
}
